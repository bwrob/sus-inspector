#tree-pane {
    width: 30%;
    border: solid $primary;
    overflow-x: hidden;
}

#detail-container {
    width: 70%;
}

#detail-pane {
    height: 1fr;
    border: solid $secondary;
    overflow-y: scroll;
}

#class-pane {
    height: 1fr;
    border: solid yellow;
    display: none;
    overflow-y: scroll;
}

#path-bar {
    dock: bottom;
    background: $accent;
}

#search-bar {
    dock: bottom;
    display: none;
}
